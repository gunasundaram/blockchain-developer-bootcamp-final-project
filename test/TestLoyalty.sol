// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/Loyalty.sol";

contract TestLoyalty {
/*
    Loyalty loyalty = Loyalty(DeployedAddresses.Loyalty());
    
    function testGetBalanceLoyaltyPoints() public {
        uint loyaltyPoints = loyalty.getBalanceLoyaltyPoints();
        Assert.equal(loyaltyPoints, 0, "Loyalty points must be 0");
    }
*/
}
